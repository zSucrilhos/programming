function friend(friends){
  var friend = [friends]
  for (var i = 0; i < friend.length; i++) {
    if (friend[i].length != 4) {
    console.log(frient[i] + "'s a foe!");
    };
  };
};

friend("Katt", "Test", "Amolith");
