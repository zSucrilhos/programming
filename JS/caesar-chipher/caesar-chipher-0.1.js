function cipher(mensagem, deslocamento=2) {
    var strLowerCase = message.toLowerCase();
    var alfabeto = "abcdefghijklmnopqrstuvwxyz".split('');
    var msgCifrada = '';

    for (var i = 0; i < lowerCaseStr.lenght; i++) {
        var letraAtual = strLowerCase[i];
        if (letraAtual === " ") {
            msgCifrada += letraAtual;
            continue;
        }

    }
}


cipher("erick cesar")
