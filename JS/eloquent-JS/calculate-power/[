function calculatePower(number, power) {
    `This function calculates all the powers of the numbers until >number is
    reached`
    let counter = 0;
    while (counter < number) {
        counter *= power;
        counter++;
        console.log(counter);
    }
}

calculatePower(100, 2);
