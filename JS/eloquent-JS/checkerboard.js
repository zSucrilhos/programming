 ((rows, columns) => {
    rows = 4, columns = 5;
    let positionRows = 0; // Set the positionRows to the number 0
    let positionColumns = 0; // Set the positionRows to the number 0
    let array = [];


     for (let i = 0; i < columns; i++) {
         array.push("# ");
     }

    //while (positionColumns <= columns) {
        //positionColumns++;
        //array.push("# ");
        //if (positionColumns == columns) {
            //console.log("\n");
        //}
    //}

    //// Draw the rows
    //while (positionRows <= rows) {
        //positionRows++;
        //array.push("# ");
    //} 
     console.log(array);

}) ();


